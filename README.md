# attendance-service-test-task
Installation:

In application.properties write path to db schema and user login and password(using PostgreSQL)

mvn spring-boot:run

Default Users:

admin admin

user user
